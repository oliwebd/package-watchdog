{
  "name": "package-watchdog",
  "version": "1.0.0",
  "description": "Checks for system and Flatpak updates and notifies you via native GNOME Shell notifications, with a top panel indicator showing live update status.",
  "main": "extension.js",
  "type": "module",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/oliwebd/package-watchdog.git"
  },
  "homepage": "https://github.com/oliwebd/package-watchdog#readme",
  "scripts": {
    "build": "mkdir -p dist && tsc && cp src/stylesheet.css dist/ && cp metadata.json dist/ && cp -r schemas dist/",
    "watch": "tsc --watch",
    "lint": "eslint 'src/**/*.ts'",
    "format": "prettier --write 'src/**/*.ts'",
    "pack": "pnpm run build && glib-compile-schemas dist/schemas/ && gnome-extensions pack --force --extra-source=utils.js --extra-source=indicator.js --extra-source=checks.js dist/",
    "deploy": "pnpm run pack && gnome-extensions install --force package-watchdog@oliwebd.github.com.shell-extension.zip && gnome-extensions enable package-watchdog@oliwebd.github.com"
  },
  "keywords": [
    "gnome-shell",
    "extension",
    "updates",
    "package-manager",
    "flatpak",
    "security",
    "vulnerability",
    "monitoring",
    "linux"
  ],
  "author": "oliwebd <oli@bdtechx.com>",
  "license": "GPL-3.0-or-later",
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.2.1",
    "eslint-config-prettier": "^10.1.8",
    "eslint-plugin-prettier": "^5.5.5",
    "globals": "^17.5.0",
    "prettier": "^3.8.3",
    "typescript": "^6.0.3",
    "typescript-eslint": "^8.59.0"
  }
}
